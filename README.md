# Chain Garden 🌱

**Web3 Botanical Generator** - Generate unique plant NFTs from audio/visual data

Version: 3.2.0 | Network: Sepolia Testnet

---

## 📁 项目架构

```
chain-garden/
├── contracts/                 # 智能合约代码
│   └── ChainGardenNFT.sol    # ERC-721 NFT合约
├── scripts/                   # 部署脚本
│   └── deploy.js             # 合约部署脚本
├── src/                       # React前端代码
│   ├── components/           # React组件
│   │   ├── MintModal.tsx
│   │   ├── PlantCanvas.tsx
│   │   └── SpecimenDetailModal.tsx
│   ├── services/             # 服务层
│   │   ├── audioService.ts   # 音频分析服务
│   │   ├── geminiService.ts  # Gemini AI服务
│   │   ├── plantMusicService.ts # 植物音乐生成
│   │   └── web3Service.ts    # Web3交互服务
│   ├── App.tsx               # 主应用组件
│   ├── index.tsx             # 入口文件
│   └── types.ts              # TypeScript类型定义
├── hardhat.config.js         # Hardhat配置
├── package.json              # 项目依赖（Hardhat）
└── README.md                 # 项目文档
```

---

## ✅ 已完成功能

### 前端功能
- ✅ **音频输入系统**
  - 麦克风实时音频捕获
  - 音频文件上传播放
  - 音频频谱分析（低音/中音/高音）

- ✅ **植物生成系统**
  - Gemini AI 驱动的植物DNA生成（基于文本描述）
  - 手动调参模式（架构、叶形、颜色、生长速度）
  - 4种生长架构：分形树、有机藤蔓、径向多肉、蕨类叶
  - 5种叶形：蕨类、圆形、针状、抽象、心形

- ✅ **可视化渲染**
  - Canvas实时植物生长动画
  - 音频驱动的动态生长效果
  - 植物音乐生成（基于DNA参数）
  - 截图保存功能

- ✅ **收藏系统**
  - 本地存储的标本库（LocalStorage）
  - 标本详情查看
  - 标本删除功能

- ✅ **Web3集成**
  - MetaMask钱包连接
  - Sepolia测试网切换
  - 钱包地址显示
  - NFT铸造界面（UI已完成）

### 智能合约
- ✅ **ChainGardenNFT.sol**
  - ERC-721标准实现
  - OpenZeppelin安全库集成
  - 可配置铸造价格
  - 可配置最大供应量
  - 元数据URI管理
  - 所有者权限管理

- ✅ **部署配置**
  - Hardhat开发环境配置
  - Sepolia测试网配置
  - 部署脚本

---

## 🚧 待完成功能

### 高优先级

1. **NFT上链功能** ⚠️ **关键任务**
   - [ ] 部署 ChainGardenNFT 合约到 Sepolia 测试网
   - [ ] 更新 `src/services/web3Service.ts` 中的合约地址
   - [ ] 实现真实的 `mintNFT()` 函数（当前为模拟）
   - [ ] 实现元数据上传到 IPFS 或中心化服务器
   - [ ] 处理交易确认和错误处理
   - [ ] 添加交易状态提示（pending/confirmed/failed）

2. **元数据管理**
   - [ ] 设置 IPFS 节点（Pinata/Infura/Web3.Storage）
   - [ ] 创建元数据JSON结构（符合OpenSea标准）
   - [ ] 实现图片上传到IPFS
   - [ ] 实现元数据JSON上传到IPFS
   - [ ] 元数据验证和格式化

3. **合约部署**
   - [ ] 配置环境变量（`.env`文件）
     - `SEPOLIA_RPC_URL`
     - `PRIVATE_KEY`
     - `ETHERSCAN_API_KEY`（可选，用于验证）
   - [ ] 执行部署：`npm run deploy:sepolia`
   - [ ] 验证合约（可选）
   - [ ] 更新前端合约地址

### 中优先级

4. **用户体验优化**
   - [ ] 交易状态实时更新
   - [ ] 铸造进度条
   - [ ] 错误信息友好提示
   - [ ] 网络切换提示
   - [ ] Gas费用估算显示

5. **功能增强**
   - [ ] 支持查看已铸造的NFT（从链上读取）
   - [ ] NFT转移功能
   - [ ] 批量铸造功能
   - [ ] 铸造历史记录

6. **测试**
   - [ ] 编写合约单元测试
   - [ ] 前端集成测试
   - [ ] 端到端测试

### 低优先级

7. **文档和部署**
   - [ ] API文档
   - [ ] 用户使用指南
   - [ ] 部署到生产环境
   - [ ] 域名和SSL配置

8. **高级功能**
   - [ ] 植物DNA的链上存储（可选）
   - [ ] 稀有度系统
   - [ ] 植物进化/变异机制
   - [ ] 社区功能

---

## 🚀 快速开始

### 前置要求
- Node.js >= 18
- MetaMask浏览器扩展
- Sepolia测试网ETH（用于Gas费）

### 目录迁移（首次设置）

如果 `frontend/` 目录仍然存在，需要将其重命名为 `src/`：

**方法1：使用迁移脚本（推荐）**
```powershell
# 在项目根目录运行
.\migrate-to-src.ps1
```

**方法2：手动重命名**
- 关闭所有可能占用文件的程序（编辑器、终端等）
- 手动将 `frontend/` 文件夹重命名为 `src/`

### 安装依赖

```bash
# 安装Hardhat依赖（根目录）
npm install

# 安装前端依赖
cd src
npm install
```

### 配置环境变量

在项目根目录创建 `.env` 文件：

```env
# Sepolia RPC URL (使用 Infura 或 Alchemy)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# 部署者私钥（不要提交到Git！）
PRIVATE_KEY=your_private_key_here

# Etherscan API Key (可选，用于合约验证)
ETHERSCAN_API_KEY=your_etherscan_key

# Gemini API Key (前端使用)
GEMINI_API_KEY=your_gemini_key
```

### 编译合约

```bash
npm run compile
```

### 部署合约

```bash
# 部署到本地Hardhat节点
npm run deploy:local

# 部署到Sepolia测试网
npm run deploy:sepolia
```

部署成功后，更新 `src/services/web3Service.ts` 中的 `CONTRACT_ADDRESS`。

### 运行前端

```bash
cd src
npm run dev
```

访问 http://localhost:3000

---

## 📝 开发指南

### 合约开发
- 合约位置：`contracts/ChainGardenNFT.sol`
- 使用 Hardhat 进行编译、测试和部署
- 遵循 OpenZeppelin 安全最佳实践

### 前端开发
- 前端代码：`src/`
- 使用 Vite + React + TypeScript
- Web3交互通过 `ethers.js` v6

### 元数据格式

NFT元数据应遵循以下JSON结构：

```json
{
  "name": "Plant Species Name",
  "description": "Plant description",
  "image": "ipfs://Qm...",
  "attributes": [
    {
      "trait_type": "Growth Architecture",
      "value": "fractal_tree"
    },
    {
      "trait_type": "Leaf Shape",
      "value": "fern"
    },
    {
      "trait_type": "Color Palette",
      "value": ["#00a651", "#0078bf", "#ff48b0"]
    }
  ],
  "dna": {
    "speciesName": "...",
    "growthArchitecture": "...",
    "branchingFactor": 0.85,
    ...
  }
}
```

---

## 🔧 技术栈

- **智能合约**: Solidity 0.8.20, Hardhat, OpenZeppelin
- **前端**: React 19, TypeScript, Vite, TailwindCSS
- **Web3**: Ethers.js v6
- **AI**: Google Gemini API
- **音频**: Web Audio API

---

## 📄 许可证

MIT License

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📞 联系方式

如有问题，请提交 Issue 或联系项目维护者。

---

**注意**: 当前NFT铸造功能为模拟实现。要启用真实上链，请完成"待完成功能"中的NFT上链任务。

